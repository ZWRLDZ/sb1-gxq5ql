export interface Product {
  id: number;
  name: string;
  originalPrice: number;
  currentPrice: number;
  brand: string;
  image: string;
  category: string;
  retailer: string;
  productUrl: string;
}